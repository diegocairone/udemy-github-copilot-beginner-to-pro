# UI Components

All UI elements in this app must use Shadcn UI components. Do not create custom components; always use Shadcn UI components.

This ensures consistency, accessibility, and leverages the established design system.